{
  "name": "recobro-contract",
  "scripts": {
    "build": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run ./scripts/deploy.js --network polygon_zkEVM_cardona && npx hardhat run ./scripts/deploy.js --network base_sepolia",
    "node": "npx hardhat node",
    "deploy-local": "npx hardhat run ./scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.6",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.13.1",
    "hardhat": "^2.22.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  }
}
